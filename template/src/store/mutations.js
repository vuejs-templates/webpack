const mutations = {
    CHANGE_NAME: (state, payload) => {
        // return new Promise((resolve, reject) => {
        // console.log(payload);
        state.name++;
        // resolve();
        // });
    }
};

export default mutations;
